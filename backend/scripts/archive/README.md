# Archived Scripts

هذه السكربتات أُرشفَت لعدم استخدامها في التطوير الحالي. يمكن الرجوع إليها للاستدلال فقط.

- **Seed:** استخدم `python -m app.core.seed_data` من مجلد backend.
- **إعادة تعيين كلمة المرور:** استخدم `python scripts/reset_admin.py`.
